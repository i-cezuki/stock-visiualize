{
  "name": "seasoning-stock-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.build.json && rm -rf dist/node_modules && mkdir -p dist/node_modules && cp -r node_modules/ulid dist/node_modules/ulid"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.632.0",
    "@aws-sdk/lib-dynamodb": "^3.632.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.14.0",
    "aws-sdk-client-mock": "^4.0.1",
    "typescript": "^5.5.4",
    "vitest": "^3.2.7"
  }
}
