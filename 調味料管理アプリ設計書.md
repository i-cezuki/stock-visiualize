# 調味料管理アプリ 設計書（MVP）

> **プロジェクト名:** Seasoning Stock
> **ドキュメント種別:** 設計書（MVP）
> **最終更新:** 2026-08-01
> **改訂履歴:**
> - 第1版: 初版
> - 第2版: 認証を Cognito ID/パスワード方式へ変更。PWA/買い物リスト/CORS/条件式/バリデーションを確定（レビュー第1回反映）
> - 第3版: **セルフサインアップ無効（管理者作成のみ）** / OPTIONS・エラー応答の CORS / 買い物リストのサーバー正規化 / SPA 用 Cognito Client 設定 / **オフライン閲覧（読み取りのみ）を MVP に追加**（レビュー第2回反映）

---

## 目次

1. [概要](#1-概要)
2. [システム構成](#2-システム構成)
3. [使用技術](#3-使用技術)
4. [認証（Amazon Cognito）](#4-認証amazon-cognito)
5. [ディレクトリ構成](#5-ディレクトリ構成)
6. [データモデル](#6-データモデル)
7. [API 仕様](#7-api-仕様)
8. [画面 / UI](#8-画面--ui)
9. [買い物リストの状態遷移](#9-買い物リストの状態遷移)
10. [オフライン方針](#10-オフライン方針)
11. [マスタデータ（カテゴリ・調味料例）](#11-マスタデータカテゴリ調味料例)
12. [デザイン方針](#12-デザイン方針)
13. [非機能要件](#13-非機能要件)
14. [MVP スコープ](#14-mvp-スコープ)
15. [将来追加したい機能](#15-将来追加したい機能)
16. [実装方針（Claude Code 向けガイドライン）](#16-実装方針claude-code-向けガイドライン)

---

## 1. 概要

### 目的

家庭内の調味料の在庫を、スマートフォンから簡単に管理する。

> 「醤油あったっけ？」
> 「甜麺醤、買う必要ある？」

こうした状況を、**外出先（買い物中）でもすぐに確認できる**ようにすることが目的。買い物中は通信が不安定なこともあるため、**直近の在庫はオフラインでも閲覧できる**ようにする。

### コンセプト

- **正確なグラム数は管理しない。** 計量の手間が続かない原因になるため。
- 代わりに、残量を **5 段階** で管理する。

  | ラベル | amountLevel |
  |--------|:-----------:|
  | 満タン | 100 |
  | 多い   | 75  |
  | 半分   | 50  |
  | 少ない | 25  |
  | なし   | 0   |

- **入力コストを最小限にし、「継続できる UX」を最優先する。**
  - タップ 1 回で残量を更新（保存ボタン不要）。
  - 追加は名前とカテゴリだけで完了。

### 利用範囲

- **非公開の個人利用アプリ。** 誰でも登録できるアプリではない。
- Cognito の **セルフサインアップは無効**とし、**管理者が作成したユーザーのみ**サインインできる（[4. 認証](#4-認証amazon-cognito)）。
- 1 ユーザー = 1 在庫。家族共有（Household）は将来機能とし、データモデルの方向性だけ拡張可能にしておく（[15. 将来追加したい機能](#15-将来追加したい機能)）。

---

## 2. システム構成

すべて AWS 上に構築し、インフラは **Terraform** で管理する。認証は Amazon Cognito を利用する。

```
                     ┌──────────────┐
                     │  CloudFront  │  ← CDN / HTTPS 配信
                     └──────┬───────┘
                            │  (静的ホスティング)
                     ┌──────▼───────┐
                     │      S3      │  ← React ビルド成果物
                     └──────┬───────┘
                            │
          React (Vite + TypeScript / PWA)
             │                        │
   ①ID/パスワードでログイン        ②JWT(IDトークン)付きで fetch
             │                        │
     ┌───────▼────────┐        ┌──────▼───────┐
     │ Amazon Cognito │        │ API Gateway  │
     │  User Pool     │◀───────│  + Cognito   │  ← JWT を検証
     │ (管理者作成のみ)│ 検証   │  Authorizer  │     (OPTIONS は対象外)
     └────────────────┘        └──────┬───────┘
                                      │
                               ┌──────▼───────┐
                               │    Lambda    │  ← 単一関数でルーティング
                               └──────┬───────┘
                                      │
                               ┌──────▼───────┐
                               │   DynamoDB   │  ← シングルテーブル
                               └──────────────┘
```

- **フロントエンド:** S3 にホスティングし、CloudFront で配信。直近の在庫は端末にキャッシュしオフライン閲覧可能（[10. オフライン方針](#10-オフライン方針)）。
- **認証:** Cognito User Pool（ID/パスワード・管理者作成のみ）でサインイン → ID トークン（JWT）を取得。
- **バックエンド:** API Gateway の Cognito Authorizer で JWT を検証（`OPTIONS` は対象外）→ Lambda（単一関数でルーティング）→ DynamoDB。
- **IaC:** Terraform ですべてのリソース（Cognito 含む）を構築。

---

## 3. 使用技術

### Frontend

- React
- TypeScript
- Vite
- TailwindCSS
- React Query（サーバー状態管理・キャッシュ・**永続化**）
- React Router
- **AWS Amplify Auth もしくは `amazon-cognito-identity-js`**（Cognito ID/パスワード認証）
- PWA 対応（ホーム画面追加・アプリシェルのオフライン起動・**直近在庫のオフライン閲覧**）

### Backend

- AWS Lambda
- TypeScript
- AWS SDK v3（`@aws-sdk/lib-dynamodb` 等）

### Database

- DynamoDB（シングルテーブル設計）

### Infrastructure

- Terraform（Cognito User Pool / User Pool Client 含む）

### Authentication

- **Amazon Cognito User Pool による ID（メールアドレス）+ パスワード認証。管理者作成ユーザーのみ。**
- 詳細は [4. 認証（Amazon Cognito）](#4-認証amazon-cognito) を参照。

---

## 4. 認証（Amazon Cognito）

> レビュー **[P0/第1回]** 対応: 公開 API を認証なし・固定ユーザーで運用しない。全 API を Cognito で保護する。
> レビュー **[P1/第2回]** 対応: 個人利用のため**セルフサインアップは無効**にする。

### 方式

- **Amazon Cognito User Pool** による **ID（メールアドレス）+ パスワード** 認証。
- **管理者作成のみ**（セルフサインアップ無効）。ユーザーは管理者が作成し、初回パスワード設定 → サインインのフロー。
- サインイン成功で **ID トークン（JWT）** を取得し、以降の API 呼び出しの `Authorization` ヘッダーに付与する。

### User Pool 設定（MVP）

| 項目 | 設定 |
|------|------|
| サインイン識別子 | email |
| 必須属性 | email |
| パスワードポリシー | 最小 8 文字・大小英字・数字を必須（MFA は任意 / MVP では無効） |
| **セルフサインアップ** | **無効（`allow_admin_create_user_only = true`）。管理者作成のみ** |
| トークン有効期限 | ID/Access: 1 時間、Refresh: 30 日（目安） |
| ホストされた UI | **使わない。** アプリ内の自前ログイン画面で完結させる |

### User Pool Client 設定（SPA 用）

> レビュー **[P2/第2回]** 対応: React SPA 向けの Client 設定を明記する。

| 項目 | 設定 |
|------|------|
| クライアントシークレット | **`generate_secret = false`**（SPA ではシークレットを持てない） |
| 認証フロー | `ALLOW_USER_PASSWORD_AUTH` / `ALLOW_REFRESH_TOKEN_AUTH`（SRP 利用時は `ALLOW_USER_SRP_AUTH`） |
| トークン保存先 | Amplify Auth の標準ストレージに準拠（既定は `localStorage`）。**XSS 対策**（CSP・入力エスケープ・依存の最小化）を前提とする |
| リフレッシュ失敗時 | キャッシュ（トークン・React Query の永続データ）を破棄し、ログイン画面へ遷移 |

### フロントエンド

- **ログイン画面**を自前で実装（Amplify Auth もしくは `amazon-cognito-identity-js`）。サインアップ画面は作らない（管理者作成のみ）。
- **パスワードリセット**（「パスワードを忘れた」→ 確認コード → 再設定）と**ログアウト**を実装する（MVP 必須）。
- 認証状態を保持し、未認証ならログイン画面へリダイレクト（React Router のガード）。
- API 呼び出し時に ID トークンを `Authorization: Bearer <idToken>` として送る。
- トークン期限切れ時はリフレッシュトークンで自動更新、失敗時はキャッシュ破棄 → 再ログインへ。

### バックエンド（認可）

- **API Gateway に Cognito Authorizer** を設定し、`GET/POST/PATCH/DELETE` で JWT を検証する（`OPTIONS` は対象外 → [7. CORS](#cors)）。
- Lambda では検証済みトークンの **`sub`（ユーザー一意 ID）** を利用してユーザーを識別する。
- **`getUserId()` はこの `sub` を返す**よう実装する（[16. 実装方針](#16-実装方針claude-code-向けガイドライン)）。DynamoDB の PK は `USER#{sub}` となり、ユーザーごとにデータが分離される。
- クライアントから userId は受け取らない。PK は必ずサーバー側で `sub` から組み立てる。他ユーザーの `id` を指定されても PK が一致せずアクセスできない。

---

## 5. ディレクトリ構成

```
root
├── frontend
│   ├── src
│   │   ├── components      # 責務単位のコンポーネント
│   │   ├── features        # 機能単位（一覧 / 買い物リスト / 認証 等）
│   │   ├── auth            # Cognito 認証（ログイン状態・トークン管理）
│   │   ├── api             # API 呼び出し層（トークン付与・通信）
│   │   ├── hooks           # カスタムフック
│   │   ├── offline         # React Query 永続化 / オフライン閲覧
│   │   ├── types           # 型定義
│   │   └── App.tsx
│   ├── public
│   └── package.json
│
├── backend
│   ├── src
│   │   ├── handler.ts      # Lambda エントリ / ルーティング
│   │   ├── auth            # トークンからの userId 取得抽象化（getUserId）
│   │   ├── repository      # Repository パターン（DynamoDB アクセス）
│   │   ├── services        # ビジネスロジック（正規化ルール含む）
│   │   └── types           # 型定義（frontend と共有できる形が望ましい）
│   ├── package.json
│   └── tsconfig.json
│
└── terraform
    ├── main.tf
    ├── cognito.tf          # User Pool（管理者作成のみ）/ Client（シークレットなし）
    ├── lambda.tf
    ├── api_gateway.tf      # Cognito Authorizer（OPTIONS 除外）/ CORS 含む
    ├── dynamodb.tf
    ├── s3.tf
    ├── cloudfront.tf
    ├── iam.tf
    └── variables.tf
```

> `src` 配下のサブディレクトリは実装時の推奨構成。過度に細分化しない（[16. 実装方針](#16-実装方針claude-code-向けガイドライン)）。

---

## 6. データモデル

### 型定義（TypeScript）

```typescript
export interface Seasoning {
  id: string;
  name: string;
  category: Category;
  icon: string;                        // 許可リストのアイコン名（サーバー管理）
  color: string;                       // HEX 形式 例: "#5B3714"（サーバー管理）
  amountLevel: 0 | 25 | 50 | 75 | 100; // 残量 5 段階
  needsPurchase: boolean;              // 買い物リスト表示フラグ
  memo?: string;
  createdAt: string;                   // ISO 8601
  updatedAt: string;                   // ISO 8601
}

export type Category = "液体" | "チューブ" | "瓶" | "粉" | "スパイス";
```

### DynamoDB（シングルテーブル）

| 項目 | 値 |
|------|----|
| テーブル名 | `Seasonings` |
| パーティションキー (PK) | `USER#{userId}`（`userId` = Cognito の `sub`） |
| ソートキー (SK) | `SEASONING#{id}` |

**キー例**

```
PK = USER#a1b2c3d4-....(Cognito sub)
SK = SEASONING#01JABC123
```

**レコード例**

```json
{
  "PK": "USER#a1b2c3d4-e5f6-....",
  "SK": "SEASONING#01JABC123",
  "id": "01JABC123",
  "name": "醤油",
  "category": "液体",
  "icon": "soy",
  "color": "#5B3714",
  "amountLevel": 75,
  "needsPurchase": false,
  "memo": "",
  "createdAt": "2026-08-01T00:00:00Z",
  "updatedAt": "2026-08-01T00:00:00Z"
}
```

### アクセスパターンと条件式

> レビュー **[P2/第1回]** 対応: DynamoDB 操作に条件式を入れ、404 / 重複防止 / 誤上書き防止を保証する。

| 操作 | 方法 | 条件式 |
|------|------|--------|
| 一覧取得 | `Query` PK = `USER#{sub}`, SK `begins_with SEASONING#` | — |
| 単一取得 | `GetItem` PK + SK | — |
| 追加 | `PutItem` | `attribute_not_exists(PK) AND attribute_not_exists(SK)` |
| 更新 | `UpdateItem`（`updatedAt` を必ず更新） | `attribute_exists(PK) AND attribute_exists(SK)` |
| 削除 | `DeleteItem` | `attribute_exists(PK) AND attribute_exists(SK)` |
| 買い物リスト | 一覧取得後、フロント側で `needsPurchase === true` を絞り込み | — |

- 条件式失敗（`ConditionalCheckFailedException`）は API 層で **`404 Not Found`**（更新・削除時）または **`409 Conflict`**（作成時の ID 衝突）にマッピングする。
- **id の採番:** ソートしやすく衝突しにくい **ULID** を推奨。

---

## 7. API 仕様

- ベースパス: `/seasonings`
- リクエスト / レスポンスは `application/json`
- **`GET/POST/PATCH/DELETE` は認証必須**（`Authorization: Bearer <Cognito ID トークン>`）。未認証は `401`。
- 対象ユーザーは常にトークンの `sub` から解決する（クライアントから userId は受け取らない）。

### 認証ヘッダー

```
Authorization: Bearer eyJraWQi...(Cognito ID Token JWT)
```

### CORS

> レビュー **[P1/第1回・第2回]** 対応: 別オリジン（CloudFront ⇄ API Gateway）のため CORS を明記し、プリフライトが認証で失敗しないようにする。

| 項目 | 設定 |
|------|------|
| Access-Control-Allow-Origin | 本番: CloudFront ドメインのみ / 開発: `http://localhost:5173`（Terraform 変数で切替） |
| Access-Control-Allow-Methods | `GET, POST, PATCH, DELETE, OPTIONS` |
| Access-Control-Allow-Headers | `Authorization, Content-Type` |
| **`OPTIONS`（プリフライト）** | **Cognito Authorizer の対象外**とし、認証なしで `200` を返す（プリフライトには `Authorization` が付かないため） |
| **エラー応答の CORS** | **`401 / 403 / 4xx / 5xx` を含む全レスポンスに CORS ヘッダーを付与**する（API Gateway の Gateway Responses / Lambda の共通レスポンスで担保） |
| 許可オリジンの切替 | Terraform 変数 `allowed_origin`（本番=CloudFront ドメイン、開発=localhost）で管理 |

### エンドポイント一覧

| メソッド | パス | 概要 |
|----------|------|------|
| GET    | `/seasonings`        | 一覧取得 |
| POST   | `/seasonings`        | 追加 |
| PATCH  | `/seasonings/{id}`   | 更新（部分更新） |
| DELETE | `/seasonings/{id}`   | 削除 |

---

### GET /seasonings — 一覧取得

ログインユーザー（`sub`）の調味料を返す。

**Response `200`**

```json
[
  {
    "id": "01JABC123",
    "name": "醤油",
    "category": "液体",
    "icon": "soy",
    "color": "#5B3714",
    "amountLevel": 75,
    "needsPurchase": false,
    "memo": "",
    "createdAt": "2026-08-01T00:00:00Z",
    "updatedAt": "2026-08-01T00:00:00Z"
  }
]
```

---

### POST /seasonings — 追加

**Request Body**

```json
{
  "name": "醤油",
  "category": "液体"
}
```

| フィールド | 必須 | 制約 |
|-----------|:---:|------|
| `name`     | ✓ | 1〜30 文字 |
| `category` | ✓ | 定義済みカテゴリのいずれか |
| `memo`     | - | 0〜200 文字 |

- `id` / `createdAt` / `updatedAt` はサーバー側で採番。
- `amountLevel` は初期値 `100`（満タン）、`needsPurchase` は `false`。
- **`icon` / `color` はサーバーがカテゴリに応じて決定**（クライアントからは受け取らない → [P2/第1回] 対応）。

**Response `201`** — 作成された `Seasoning` を返す。

---

### PATCH /seasonings/{id} — 更新

**Request Body（更新したいフィールドのみ）**

```json
{
  "amountLevel": 50,
  "needsPurchase": false
}
```

> レビュー **[P2/第1回]** 対応: `icon` / `color` は MVP では **サーバー管理とし、更新対象から除外**する。

| フィールド | 更新可 | 制約 |
|-----------|:---:|------|
| `name`          | ✓ | 1〜30 文字 |
| `category`      | ✓ | 定義済みカテゴリのいずれか（変更時、`icon`/`color` はサーバーが再計算） |
| `amountLevel`   | ✓ | `0 / 25 / 50 / 75 / 100` のいずれか |
| `needsPurchase` | ✓ | boolean |
| `memo`          | ✓ | 0〜200 文字 |
| `icon` / `color`| ✗ | 更新不可（サーバー管理） |

**サーバー側の整合性ルール（必須）**

> レビュー **[P1/第2回]** 対応: `amountLevel=0` と `needsPurchase` の矛盾をサーバーで防ぐ。

- PATCH 適用後に **`amountLevel === 0` の場合は `needsPurchase = true` に正規化**する（クライアント指定が `false` でも上書き）。
- 「買い物リストで購入済みにする（`needsPurchase=false`）」場合は、**同時に `amountLevel` を 1 以上（通常は `100`）へ更新する**運用とする。`amountLevel=0` のまま `needsPurchase=false` は保存させない。
- サーバーは `updatedAt` を必ず現在時刻で更新する。
- 存在しない `id`（PK 不一致含む）は `404`。

**Response `200`** — 更新後の `Seasoning` を返す（正規化後の値）。

---

### DELETE /seasonings/{id} — 削除

- 存在しない `id`（PK 不一致含む）は `404`。

**Response `204`** — ボディなし。

---

### エラーレスポンス（共通）

すべてのエラー応答にも **CORS ヘッダーを付与**する。

| ステータス | 意味 | 例 |
|-----------|------|----|
| `400` | バリデーションエラー | 必須項目欠落 / 文字数超過 / 不正なカテゴリ / 不正な `amountLevel` |
| `401` | 未認証 | トークンなし / 無効 / 期限切れ |
| `404` | 対象が存在しない | 指定 `id` の調味料が無い（他人のデータ含む） |
| `409` | 競合 | 作成時の ID 衝突 |
| `500` | サーバーエラー | DynamoDB 障害等 |

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "name is required"
  }
}
```

---

## 8. 画面 / UI

スマートフォンファースト。画面は最小限に絞る。

### 画面一覧

| 画面 | 内容 |
|------|------|
| ログイン | Cognito ID/パスワード。パスワードリセット導線を含む（サインアップ画面はなし） |
| 一覧（ホーム） | 調味料カードの一覧。残量表示・買い物トグル。オフライン時は直近データを表示 |
| 残量変更 | カードタップで 5 段階選択（即保存） |
| 追加 | 名前 + カテゴリ入力 |
| 買い物リスト | `needsPurchase === true` のみ表示 |

### 一覧画面（カード表示）

```
┌─────────────┐   ┌─────────────┐
│     🧴      │   │     🫙      │
│    醤油     │   │   甜麺醤    │
│ █████░ 75%  │   │ ░░░░░░ 0%   │
│   まだある   │   │   なし 🛒   │
└─────────────┘   └─────────────┘
```

- 各カードにアイコン・名前・残量バー・残量ラベルを表示。
- 買い物リスト対象（`needsPurchase === true`）はカードにアイコン（🛒 等）を表示。
- カードをタップすると残量変更に遷移。

### 残量変更

```
醤油
 ○ 満タン
 ○ 多い
 ● 半分
 ○ 少ない
 ○ なし
```

- 選択すると **即保存**（保存ボタンは不要）。
- 保存後、カードの残量バーがアニメーションして反映される（[12. デザイン方針](#12-デザイン方針)）。

### 削除の UX

> レビュー **[P2/第1回]** 対応: 誤削除を防ぐ。

- 削除前に **確認ダイアログ** を表示する。
- 可能なら削除直後に **Undo（取り消し）** を用意する。
- 削除 API 成功後、React Query のキャッシュから該当アイテムを除去して即時反映する。

### 基本 UI 状態（全画面共通）

| 状態 | 表示 |
|------|------|
| ローディング | スケルトン or スピナー |
| 空（データ 0 件） | 「調味料がまだありません。追加しましょう」＋追加導線 |
| エラー | 再試行ボタン付きのエラーメッセージ |
| オフライン | 「オフライン（最終更新: HH:MM）」を表示し、**直近の在庫一覧を表示**（空表示に落とさない） |

---

## 9. 買い物リストの状態遷移

> レビュー **[P1/第1回・第2回]** 対応: 買い物リストの生成・購入済み操作を確定し、サーバー側でも整合性を保証する。
> **方針: 手動トグル ＋ 残量 0 で自動フラグ（サーバー正規化）。**

### `needsPurchase` の遷移ルール

| きっかけ | 挙動 |
|----------|------|
| 残量を **なし（0）** に変更 | サーバーが自動で `needsPurchase = true`（[7. PATCH の整合性ルール](#patch-seasoningsid--更新)） |
| 一覧カードの買い物トグルを ON | 手動で `needsPurchase = true` |
| 一覧カードの買い物トグルを OFF | 手動で `needsPurchase = false`（ただし `amountLevel=0` のままは不可 → 残量も更新） |
| 買い物リストで **購入済みチェック** | `needsPurchase = false` ＋ `amountLevel` を 1 以上（通常 100）へ更新 |

- 自動フラグは **クライアント任せにせず、PATCH 後にサーバーで正規化**する（矛盾状態を保存させない）。
- 購入済みチェック時、`amountLevel` を `100`（満タン）に戻すのが既定。別の残量にしたい場合は残量変更で調整。

### 画面（買い物リスト）

```
買い物リスト
 □ 甜麺醤     （なし）
 □ 豆板醤     （少ない）
 □ オリーブオイル（なし）
```

- チェックすると `needsPurchase=false`（＋残量更新）にして一覧から消える。
- 実装: 一覧取得結果を `needsPurchase === true` でフィルタして表示。

---

## 10. オフライン方針

> レビュー **[P2/第2回]** ＋ 追加要望対応: 買い物中の通信不安定に備え、**直近の在庫を読み取り専用でオフライン閲覧できる**ようにする。

### MVP で対応する範囲

| 区分 | MVP | 方針 |
|------|:---:|------|
| アプリシェルのオフライン起動 | ✓ | Service Worker でシェル（HTML/JS/CSS）をキャッシュ |
| **直近在庫のオフライン閲覧** | ✓ | 一覧取得成功時のデータを永続化し、オフライン/通信失敗時に表示 |
| オフラインでの追加・編集・削除・残量変更 | ✗ | MVP 対象外（オンライン時のみ）。将来対応（[15. 将来](#15-将来追加したい機能)） |

### 実装方針

- **React Query の永続化**（`persistQueryClient` + IndexedDB/localStorage）で `GET /seasonings` の結果を端末に保存する。
- 起動時・オフライン時・通信失敗時は、**空表示に落とさず直近データを表示**し、`updatedAt` 由来の「最終更新時刻」を画面に示す。
- オフライン中は残量変更・追加・削除などの**更新系 UI を無効化**（または「オンライン時のみ」と表示）する。矛盾した楽観更新は行わない。
- 復帰時（オンライン化）に自動で再取得し、最新に更新する。

---

## 11. マスタデータ（カテゴリ・調味料例）

### カテゴリ

- 液体
- チューブ
- 瓶
- 粉
- スパイス

### 調味料の例（登録時のサジェスト用）

醤油 / みりん / 酒 / 酢 / ごま油 / オリーブオイル / 味噌 / 甜麺醤 / 豆板醤 / コチュジャン / ケチャップ / マヨネーズ / ソース / ポン酢 / 白だし / コンソメ / 鶏ガラスープ / 塩 / 砂糖 / 胡椒

- MVP では **アプリ内サジェスト（フロントエンド定数）** として持つ。初回投入データ（DB シード）は行わない。

### icon / color のマスタ

- `icon` は許可リスト方式（例: `soy`, `oil`, `paste`, `powder`, `spice` …）。
- `color` は HEX 形式でカテゴリごとに既定値を定義。
- どちらも **サーバー管理**とし、カテゴリから決定する（クライアントからの任意指定は不可 → [P2/第1回] 対応）。

---

## 12. デザイン方針

### カード UI

- 角丸
- 影（軽いドロップシャドウ）
- アイコン表示
- **ボトルの残量を色で可視化**
- `amountLevel` に応じて、**下から色が満たされる**アニメーション

```
残量イメージ（下から満ちる）

  ┌──────┐        ┌──────┐
  │      │        │██████│
  │      │        │██████│
  │██████│        │██████│
  │██████│        │██████│
  └──────┘        └──────┘
   50%             100%
```

- 残量変更時はアニメーションで遷移させる。

### トーン & マナー

- シンプルで、**Apple 純正アプリのようなミニマルな雰囲気**を目指す。
- ダークモード対応。
- アクセシビリティ（コントラスト・タップ領域・スクリーンリーダー）を考慮。

---

## 13. 非機能要件

| 項目 | 方針 |
|------|------|
| 対応デバイス | スマートフォンファースト（レスポンシブ） |
| 認証 | Cognito ID/パスワード（管理者作成のみ）。`GET/POST/PATCH/DELETE` を JWT で保護 |
| PWA | アプリシェルのオフライン起動＋**直近在庫のオフライン閲覧（読み取りのみ）**（[10. オフライン方針](#10-オフライン方針)） |
| パフォーマンス | 一覧取得は 1 リクエストで完結。React Query でキャッシュ＋永続化 |
| ダークモード | 対応 |
| アクセシビリティ | コントラスト比・タップ領域・aria 属性を考慮 |
| セキュリティ | 認証必須・セルフサインアップ無効・ユーザーデータ分離（PK に `sub`）・CORS を本番ドメインに限定・トークン保存時の XSS 対策 |

---

## 14. MVP スコープ

### 必須（MVP に含む）

- **Cognito ID/パスワード認証（管理者作成のみ）**
  - ログイン / **パスワードリセット** / **ログアウト**
- 調味料一覧
- 調味料追加
- 編集
- 削除（確認ダイアログ付き）
- 残量変更
- 買い物リスト（手動トグル＋残量 0 で自動、サーバー正規化）
- スマホ対応
- PWA（アプリシェルのオフライン起動）
- **直近在庫のオフライン閲覧（読み取りのみ）**

### 対象外（MVP に含まない）

- オフラインでの更新（追加・編集・削除・残量変更）
- 家族共有（Household）
- OCR / バーコード / AI 画像認識
- 通知
- 履歴
- レシート読み取り

---

## 15. 将来追加したい機能

| 機能 | 内容 |
|------|------|
| 家族共有 | `Household` 概念を追加（`PK = HOUSEHOLD#{id}`）。Cognito ユーザーを世帯に紐付け |
| オフライン更新 | オフライン中の残量変更・追加・削除をキューし、オンライン復帰時に同期（競合解決含む） |
| セルフサインアップ | 一般公開する場合。招待コード / 許可メールドメイン / WAF・レート制限をセットで導入 |
| MFA | Cognito の多要素認証を有効化 |
| 更新履歴 | 残量推移を保存し、月間消費量を可視化 |
| 通知 | 残量 25% 以下で通知 |
| レシピ連携 | 作りたい料理から不足している調味料を表示 |
| バーコード | 商品を読み取って登録 |
| OCR | レシートから自動追加 |
| AI | 調味料の写真から候補を推定 |

---

## 16. 実装方針（Claude Code 向けガイドライン）

以下を重視して実装すること。

**アーキテクチャ / 品質**

- 保守性を最優先する。
- TypeScript の型安全を重視する。
- React Hooks を適切に利用する。
- **Atomic Design は採用しない。** コンポーネントは責務単位で分割する。
- API 層を分離する。
- **Repository パターン**を採用する（DynamoDB アクセスを Repository に隔離）。
- DynamoDB 操作には**条件式**を必ず付け、エラーを適切な HTTP ステータスにマッピングする（[6. データモデル](#6-データモデル)）。
- **買い物リストの自動フラグ（`amountLevel=0 → needsPurchase=true`）はサーバーの Service 層で正規化**する。フロントの表示ロジックだけに頼らない。

**認証 / 認可**

- **Cognito ID/パスワード認証を実装する**（フロント: ログイン/パスワードリセット/ログアウト、バック: API Gateway Cognito Authorizer）。
- **User Pool はセルフサインアップ無効（管理者作成のみ）**、**User Pool Client はクライアントシークレットなし（`generate_secret = false`）** とする。
- **`getUserId()` は検証済み JWT の `sub` を返す**よう実装し、ユーザー識別を一箇所に抽象化する。将来 Household 化する際もここを差し替えれば済むようにする。
- クライアントから userId を受け取らない。PK は必ずサーバー側で `sub` から組み立てる。
- リフレッシュ失敗時はトークンと永続キャッシュを破棄してログイン画面へ。
- 入力バリデーション（文字数・カテゴリ・`amountLevel` の許可値）をサーバー側で必ず行う。

**インフラ / バックエンド**

- Terraform はモジュール化しすぎない。ただし Cognito（User Pool / Client）、API Gateway の Authorizer・CORS を定義する。
- **`OPTIONS` は Cognito Authorizer の対象外**にし、認証なしで `200` を返す。
- **全レスポンス（エラー含む）に CORS ヘッダーを付与**する（Gateway Responses / Lambda 共通レスポンス）。
- Lambda は単一関数でルーティングする。
- CORS の許可オリジンは Terraform 変数（本番 CloudFront ドメイン / 開発 `localhost`）で切り替える。

**フロントエンド / UX**

- UI はスマートフォンファースト。
- ダークモード対応。
- アクセシビリティを考慮する。
- 削除は確認ダイアログ（可能なら Undo）を入れる。
- ローディング / 空 / エラー / **オフライン（直近データ＋最終更新時刻）** の基本状態を各画面で用意する。
- **React Query の永続化**で直近在庫をオフライン閲覧可能にし、オフライン中は更新系 UI を無効化する。
- デザインはシンプルで、Apple 純正アプリのようなミニマルな雰囲気を目指す。
