# 調味料管理アプリ レビュー（第2回）

レビュー対象: `調味料管理アプリ設計書.md`  
レビュー日: 2026-08-01  
観点: 更新後の MVP 設計レビュー（認証、CORS、API 整合性、UX、運用）

## 総評

前回レビューの大きな懸念だった「認証なし + `USER#default` 固定」は、Cognito User Pool と `sub` ベースの PK に変更されており、公開 API としての最低限の安全性はかなり改善されています。PWA の範囲、買い物リストの状態遷移、CORS、DynamoDB 条件式、`icon` / `color` のサーバー管理も追記され、MVP として実装に進めやすい設計になりました。

残っている主な論点は、Cognito を入れたことで新しく発生する運用・実装上の詰めです。特に「個人利用なのにセルフサインアップを公開するか」「CORS プリフライトを Cognito Authorizer の対象にしてしまわないか」「買い物リストの状態遷移をサーバー側でも保証するか」は、実装前に明文化した方が安全です。

## 指摘事項

### [P1] 個人利用前提なのにセルフサインアップ有効だと、誰でもアカウントを作れてしまう

該当箇所: `調味料管理アプリ設計書.md` 155, 589, 604

設計では「当面は個人利用のみ」としつつ、Cognito のセルフサインアップは有効になっています。データは `sub` で分離されるため他人の在庫は読めませんが、公開アプリとして誰でもユーザー登録できる状態になり、不要な Cognito ユーザー作成、API 利用、DynamoDB 書き込み、コスト増の余地が残ります。

推奨:

- 個人利用 MVP なら、セルフサインアップは無効にして管理者作成ユーザーだけにする。
- セルフサインアップを残すなら、招待コード、許可メールドメイン、WAF / レート制限などの制御を設計に追加する。
- 「個人利用」と「一般公開可能」のどちらを MVP の前提にするかを明記する。

### [P1] Cognito Authorizer を全エンドポイントに掛けると、OPTIONS プリフライトまで認証される恐れがある

該当箇所: `調味料管理アプリ設計書.md` 168, 303-312, 665-667

「全エンドポイントで JWT を検証」と「OPTIONS に 200 を返す」が併記されています。Terraform 実装時に `OPTIONS /seasonings` や `OPTIONS /seasonings/{id}` にも Cognito Authorizer を付けてしまうと、ブラウザのプリフライトリクエストには `Authorization` が付かないため、CORS が失敗してフロントエンドから API を呼べません。

推奨:

- `OPTIONS` は Authorizer なしで 200 を返す、と API / Terraform 方針に明記する。
- `401` / `403` / `4xx` / `5xx` のエラーレスポンスにも CORS ヘッダーを付ける。
- 本番 CloudFront ドメインと開発 `localhost` の切り替え方法を Terraform 変数として定義する。

### [P1] `amountLevel = 0` で `needsPurchase = true` にするルールをサーバー側で保証するかが未定義

該当箇所: `調味料管理アプリ設計書.md` 375-398, 506-512, 658-661

買い物リストの仕様では、残量を `0` にしたら自動で `needsPurchase = true` になります。一方、PATCH API では `amountLevel` と `needsPurchase` が独立して更新可能です。クライアントの実装だけで自動フラグを行うと、API から `{ "amountLevel": 0, "needsPurchase": false }` のような矛盾した状態を保存できる可能性があります。

推奨:

- サーバー側のビジネスルールとして、PATCH 後の `amountLevel === 0` なら `needsPurchase = true` に正規化する。
- もしくは `amountLevel=0` と `needsPurchase=false` の同時指定を `400` にする。
- 購入済みチェック時だけ例外的に `amountLevel=0` かつ `needsPurchase=false` を許可するなら、専用エンドポイントまたは明確な操作種別を設ける。

### [P2] SPA 用 Cognito User Pool Client の設定が不足している

該当箇所: `調味料管理アプリ設計書.md` 156-164, 665

React SPA から Cognito を使う場合、User Pool Client はクライアントシークレットなしで作る必要があります。また、トークンをどこに保存するか、リフレッシュトークンをどう扱うかが未定義だと、XSS 時の被害やログイン永続化の挙動が実装者依存になります。

推奨:

- User Pool Client は `generate_secret = false` と明記する。
- トークン保存先は Amplify の標準に寄せる、または保存方針と XSS 対策を明記する。
- パスワードリセット、ログアウト、リフレッシュ失敗時のキャッシュ破棄も MVP の認証仕様に追加する。

### [P2] PWA はオンライン前提と整理されたが、外出先確認の期待値とのズレは残る

該当箇所: `調味料管理アプリ設計書.md` 36, 590, 596, 616

今回の更新で「在庫データのオフライン閲覧 / 更新は対象外」と明記されたため仕様の曖昧さは解消されています。ただし、アプリの目的は買い物中の確認なので、店舗内で通信が悪い場合に在庫を見られない可能性は引き続き UX リスクです。

推奨:

- MVP ではオンライン前提でよいが、一覧取得成功時の React Query キャッシュを画面遷移中に保持することは必須にする。
- 通信失敗時に空表示へ落とさず、直前データがあれば「最終更新時刻つき」で表示する方針を検討する。
- ユーザーテストで買い物中の通信環境を確認し、必要ならオフライン閲覧を早めに繰り上げる。

## 解消済み

- 認証なし・固定 `USER#default` の公開 API リスクは、Cognito + `sub` ベースの PK で解消方向。
- `icon` / `color` の任意更新リスクは、サーバー管理に変更されて解消。
- DynamoDB の更新 / 削除 404 と作成時衝突は、条件式とエラーマッピングが追加されて解消。
- 削除 UX は、確認ダイアログと Undo 方針が追加されて改善。
- 買い物リストは、手動トグル + 残量 0 自動フラグとして仕様がかなり明確化。

## 確認したい点

- MVP は「自分だけが使う非公開アプリ」か、「URL を知っている人も登録できるアプリ」か。
- `OPTIONS` と API Gateway のエラーレスポンスに CORS ヘッダーを付ける実装方針を Terraform に明記するか。
- 買い物リストの自動フラグをフロントだけで行うか、サーバーでも正規化するか。
- Cognito のパスワードリセットとログアウトは MVP 必須に含めるか。

## 推奨する次アクション

1. 個人利用 MVP なら Cognito のセルフサインアップを無効化する。
2. `OPTIONS` は Authorizer なし、エラー応答にも CORS ヘッダー付与、と設計書に追記する。
3. `amountLevel=0` と `needsPurchase` の整合性をサーバー側ルールとして定義する。
4. SPA 用 User Pool Client 設定（クライアントシークレットなし、トークン保存、ログアウト、パスワードリセット）を追加する。
5. 買い物中の通信不安定時に、直前データを表示するかどうかを UX 方針として決める。

